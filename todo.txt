- collect and show statistics (why the solution is good)

- delete from frontend the history logs
- frontend change name of models to be more sugestive

- each client works only one network
- TLS - needs changes when in production  + k3s


Optional:
- make the ai check for critical cases and anounce the doctors
- make the server ( which is single point of failure) safer (second server or other ways)
- weights - reverse attack - to make for a better security

TODO THesis:
- change C2 diagrams font ( make it bigger ) - grpc not rpc, not nginx
- change things regarding ai ( after calibratio changes )
write about kubernetes/cloudflare after site is deployed + deployment diagram
bibliography -add django, django guardian docs etc



DONE


Redis used for :
notifications when appointments are assigned
periodic deletion of appointments
checking critical cases

RBAC - access control
TLS
mapping user - data with HMAC


Training
- downweight already trained on patients